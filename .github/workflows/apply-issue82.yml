name: apply-issue82
on:
  push:
    branches: [feat/pareto-mixed-optimizer]
permissions:
  contents: write
jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/pareto-mixed-optimizer
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
      - name: Apply compiler integration
        run: |
          python3 tools/apply_issue82_compile.py
          python3 tools/fix_issue82_compile.py
      - name: Format touched Rust
        run: rustfmt --edition 2024 logan-ir/src/optimizer.rs logan-ir/src/plan.rs logan-ir/src/lib.rs logan-compiler/src/optimizer.rs logan-compiler/src/pipeline.rs logan-compiler/src/cli.rs logan-compiler/src/lib.rs
      - name: Test focused crates
        run: cargo test -p logan-ir -p logan-compiler --all-targets
      - name: Remove one-shot machinery
        run: rm tools/apply_issue82_compile.py tools/fix_issue82_compile.py .github/workflows/apply-issue82.yml
      - name: Commit result
        run: |
          git config user.name 'logan-issue82'
          git config user.email 'actions@users.noreply.github.com'
          git add -A
          git commit -m 'feat: implement Pareto mixed representation optimizer'
          git push origin HEAD:feat/pareto-mixed-optimizer
