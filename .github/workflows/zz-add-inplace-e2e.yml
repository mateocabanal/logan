name: add-inplace-e2e-test

on:
  push:
    branches:
      - fix/true-low-space-inplace-recompile

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/true-low-space-inplace-recompile
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
      - name: Add test
        run: python3 tools/zz_add_inplace_e2e.py
      - name: Format recompiler only
        run: rustfmt --edition 2024 logan-compiler/src/recompile.rs
      - name: Test compiler
        run: cargo test -p logan-compiler --all-targets
      - name: Commit test
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add logan-compiler/src/recompile.rs
          git rm -f tools/zz_add_inplace_e2e.py .github/workflows/zz-add-inplace-e2e.yml || true
          git commit -m 'test: cover true low-space in-place retarget'
          git push origin HEAD:fix/true-low-space-inplace-recompile
